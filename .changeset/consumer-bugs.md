---
"pkglab": patch
---

Fix regex lastIndex bug in lockfile sanitization, propagate install errors in verbose mode, handle catalog restore/rollback for freshly-added packages, and use per-package oldVersion for pnpm lockfile patching
